# Ark Guidelines

- Do not rename or remove `ctx.arc(...)` in the canvas animation code.
- `ctx.arc()` is a JavaScript Canvas API call and must remain unchanged.
- Keep the current video and section background mapping unless a deliberate update is requested.
- Preserve the transparent overlay styling for the hero and section backgrounds.